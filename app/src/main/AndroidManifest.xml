<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.theory.emhwang.bluetoothproject">

    <!-- BLUETOOTH // 커넥션 요구, 수락, 데이터 전송등의 블루투스 통신을 하기 위해 필요한 권한
         BLUETOOTH_ADMIN // 디바이스 검색 시작, 블루투스 설정을 조작하기 위해 필요한 권한-->

    <!-- Bluetooth Permission -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <!--비콘 쓰니까 로케이션 퍼미션도 받아야함-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--디바이스가 Bluetooth를 지원하는지 확인(Bluetooth 통신을 사용하기 전에)-->
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--Bluetooth 기기 검색(통신 가능한 디바이스 목록 다이얼로그 뜸)-->
        <activity
            android:name=".activity.DeviceListActivity"
            android:theme="@android:style/Theme.DeviceDefault.Dialog" />

        <!--페어링 된 기기 목록 불러옴
        (페어링된 상태 : 단말과 이미 한번 이상 연결된 기기)-->
        <activity android:name=".activity.ListActivity" />

        <!--Bluetooth 설정화면으로 이동-->
        <activity android:name=".activity.SettingActivity" />

        <!--Bluetooth 연결-->
        <activity android:name=".activity.ConnectActivity" />

        <!--BroadcastReceiver 등록-->
        <receiver android:name=".services.BluetoothReceiver">
            <!--<intent-filter>-->
                <!--&lt;!&ndash;action으로 해당 컴포넌트가 어떤 작업을 처리할 수 있는지 정의&ndash;&gt;-->
                <!--<action android:name="com.theory.emhwang.bluetoothproject.SEND_BLE_BROAD_CAST" />-->
            <!--</intent-filter>-->
        </receiver>

    </application>

</manifest>